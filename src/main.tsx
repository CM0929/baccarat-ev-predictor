import React from 'react';
import ReactDOM from 'react-dom/client';
import BaccaratEVPredictor from './BaccaratEVPredictor';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <BaccaratEVPredictor />
  </React.StrictMode>
);
